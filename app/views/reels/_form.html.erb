<%= semantic_form_for([parent, resource], :validate => true) do |form| %>
  <%= form.inputs do %>
    <%= form.input :name %>
  <% end %>

  <%= form.buttons %> or <%= link_to 'cancel', dvd_reels_path(parent) %>
<% end %>
